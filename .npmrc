# vite-plugin-pwa@1.x peer range lists Vite ≤7; this project uses Vite 8 (supported at runtime).
# Without this, `npm install` fails with ERESOLVE. Revisit when vite-plugin-pwa declares Vite 8 peers.
legacy-peer-deps=true
